# 项目约定（cadence）

学习决策与跟进 Agent。规格见 `docs/SPEC.md`，当前状态与下一步见 `HANDOFF.md`。

## 项目交接

- 使用全局 Skill `$maintain-project-handoff` 创建、审查和维护交接文档，交接入口为 `HANDOFF.md`。
- 项目事实发生实质变化时，由 Agent 在某一开发阶段结束后自动更新交接文档(HANDOFF.md并不是每一轮对话都要整理，在用户明确触发或者是完成一个阶段的时候触发)；仅回答概念问题且事实未变化时不机械更新。
- 开工前完整读取交接文档并核对 Git、相关代码和证据有效范围；相关事实未变化时可沿用既有证据，不做无触发原因的全量复验。
- 工作区没有外部附件不代表附件从未提供；历史阻塞不得当作当前阻塞，除非满足文档记录的重新打开条件。
- 合并代码不代表获准删除分支或工作树；删除必须得到用户明确授权。
- 交接文档不得记录任何密钥、令牌值或前缀，只记录环境变量名称和配置状态。


## 验证纪律（2026-09-15 定：本项目现阶段刻意从轻）

项目规模小、单用户、本地跑，验证按**最低必要**做，不为求稳而叠层。

- 后端改动：跑 `pytest -q`；只有碰了接口契约或台账写入语义时，再跑 `tools\smoke_p1.py`。
- 前端改动：跑 `npm run lint` 与 `npx tsc --noEmit`（各几秒，成本极低）。
- 前端"到底通没通"由**用户手工走查**（SPEC 第 10 节已定：前端暂不写自动化测试）。Agent 不再代为用无头浏览器、临时脚本或真实浏览器复验。
- 只有出现失败、风险扩大（改契约、改依赖、改跨模块行为）或用户明确要求时，才升级验证。
- 证据写进 `HANDOFF.md`：记确切命令，失效条件按**具体模块 + 具体行为**写，不写"碰了某目录就全废"——那会逼出没必要的全量复验。

## 教学协议（解读代码、讲解项目时优先级最高；开发任务全程生效）

我看不懂你写的代码，所以你的产出必须自带"让我变懂"的义务：

1. 全景优先、细节靠后：讲任何项目，第一步先回答"它怎么跑起来"——启动入口、请求怎么流、
   数据谁持有、文件谁 import 谁、对外借了什么。我拿到骨架之前，禁止逐行讲实现。
2. 分清现状与蓝图：先说明"代码此刻实现到哪、文档里哪些只是计划"，不把设计当事实讲。
   不存在的连线就明说不存在。
3. 抽象层级对齐"我能不能动手"：每条知识配一个我能亲手执行/验证的动作；
   我不需要掌握的部分，明说"这块你不用懂"，不许堆料。
4. 动手练习一次一幕：给我可粘贴的命令，我跑完贴输出，你核对真实结果再讲下一幕；
   收尾要求我用自己的话复述，复述错了当场纠正——我讲出来才算懂。

