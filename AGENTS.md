# 项目约定（cadence）

学习决策与跟进 Agent。规格见 `docs/SPEC.md`，当前状态与下一步见 `HANDOFF.md`。

## 项目交接

- 使用全局 Skill `$maintain-project-handoff` 创建、审查和维护交接文档，交接入口为 `HANDOFF.md`。
- 交接文档只在**未来行动所需的事实**变化时更新（状态行、证据有效性、接口契约、已知缺口、下一步、git 基线），由 Agent 在本轮结束前完成，且只做定点编辑（改受影响的那几行）；纯样式、纯重构等无行为变化的小修**不更新**——提交信息足够；仅回答概念问题且事实未变化时不机械更新。
- **每个窗口开工前**完整读取交接文档并核对 Git、相关代码和证据有效范围（同一窗口内不重复整读）；相关事实未变化时可沿用既有证据，不做无触发原因的全量复验。
- 交接文档每次更新后跑一遍校验并按输出处理（尤其篇幅）：`python C:\Users\123\.agents\skills\maintain-project-handoff\scripts\validate_handoff.py HANDOFF.md`（在仓库根目录执行），预期末行 SUMMARY 为 errors=0、warnings=0。
- 工作区没有外部附件不代表附件从未提供；历史阻塞不得当作当前阻塞，除非满足文档记录的重新打开条件。
- 合并代码不代表获准删除分支或工作树；删除必须得到用户明确授权。
- 交接文档不得记录任何密钥、令牌值或前缀，只记录环境变量名称和配置状态。

## 验证纪律（2026-09-15 定：本项目现阶段刻意从轻）

项目规模小、单用户、本地跑，验证按**最低必要**做，不为求稳而叠层。

- 后端改动：跑 `pytest -q`；只有碰了接口契约或台账写入语义时，再跑 `tools\smoke_p1.py`。
- 前端改动：跑 `npm run lint` 与 `npx tsc --noEmit`（各几秒，成本极低）。
- 前端"到底通没通"由**用户手工走查**（SPEC 第 10 节已定：前端暂不写自动化测试）。Agent 不再代为用无头浏览器、临时脚本或真实浏览器复验。
- 只有出现失败、风险扩大（改契约、改依赖、改跨模块行为）或用户明确要求时，才升级验证。
- 证据写进 `HANDOFF.md`：记确切命令，失效条件按**具体模块 + 具体行为**写，不写"碰了某目录就全废"——那会逼出没必要的全量复验。

## 沟通与系统认知协议（全程生效）

我负责把控业务需求与修改方向，技术实现由你兜底。为了保证高效协作，交流执行以下约定：

1. **动态控制篇幅**：
   - 修复 Bug、执行常规任务：一律默认短回复（3~5 句话），只说明“改动效果、当前状态、我接下来在界面该点什么/怎么操作”。
   - 严禁每轮都把底层排查流水账、排错过程倾倒给我。

2. **讲解重全景机制，不堆代码底层**：
   - 当需要向我介绍系统时，讲清楚“系统整体怎么运转、数据如何流转、各模块负责什么、现状与规划的边界”，让我理解整体运作机制。
   - 严禁倾倒底层实现碎片（如数据库字段名、内存临时状态、HTTP 状态码、SPEC 决策编号等），用通俗的业务语言表达。

3. **需求决策直接出选择题**：
   - 当面临需要我拍板的方案时，用大白话说明场景，直接列出选项（A/B 方案的体验差异与代价），并给出你的明确推荐。
