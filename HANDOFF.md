# cadence 交接文档

> 最近更新：2026-09-15 12:40（本地时区）
> 仓库根目录：`D:\cadence`
> 主工作树：`D:\cadence`｜`master`｜`5d1579e`｜无远端｜有未提交改动（见第 3 节）
> 代码基线：`5d1579e`（P1 + T19 + `backend/tools/`；每条证据的适用范围见第 5 节）
> 其他工作树：无
> 当前唯一目标：P1（后端闭环）**已完成**并由用户亲手验收；下一步待用户指定（候选：写 README / 整理文档 / 进 P2 前端）
> 下一条动作：用户跑 `pytest -q`（预期 `88 passed`）与 `tools\smoke_p1.py`（预期 9 步全绿），并复核 `/docs` 双击第二次得 `409`

## 1. 当前状态

| 范围 | 状态 | 证据依据 | 有效范围/失效条件 |
|---|---|---|---|
| 后端 P0 地基（schema / ledger） | 通过 | 沿用 E-01 | `ledger.py`、`schema.sql` 改动后失效 |
| P1 规则层（状态机 / 报告与落后量 / 周检查点判定，T4–T6） | 通过 | 本轮 E-06 | `plan.py`、`main.py` 改动后失效；T6 只有单测，未接定时任务（P4 才接） |
| 建节点防重复提交（T19） | 通过 | 本轮 E-06、E-07 | `plan.py`、`main.py` 改动后失效 |
| 服务启动与接口契约 | 通过 | 本轮 E-07、E-09 | `main.py` 改动后失效 |
| 端到端闭环（脚本可复跑） | 通过 | 本轮 E-09 | `plan.py`、`main.py` 或接口契约变更后失效 |
| 端到端闭环（用户亲手在 `/docs` 走通） | 通过 | 沿用 E-05（基线 `7eee000`；T19 后由 E-09 复现） | 用户库数据可随时复查 |
| `backend/tools/` 两个自查脚本 | 通过 | 本轮 E-08、E-09 | 脚本自身或 `plan.py` 变更后失效 |
| 前端 `frontend/` | 未验证 | 未实现 | 无 |
| SPEC 第 9 节真实使用验收 | 未验证 | 成功标准 1、5 的后端部分已由 E-05、E-09 覆盖；2、3、4 与前端部分未做 | 需 P2–P4 完成后 |

## 2. 当前目标与完成定义

**目标：P1（后端闭环）——已完成。** 完成定义是「在 `/docs` 建计划与两级节点 → 提交报告 → 节点状态按状态机推进、落后量可见 → 台账留流水 → 阶段收尾产出推进提案」，已由用户亲手走通（E-05），并在 T19 之后由脚本复现（E-09）。

**本轮不做：** 前端（P2）、LLM 与 provider 管理（P3）、触达与 Markdown 导出（P4）。

**下一步（待用户指定）：** 写项目 README ／ 整理文档 ／ 进 P2 前端（需先装 npm 依赖，属 SPEC 第 16 节的 Ask first）。

## 3. 当前开放问题

无阻塞问题。**仅剩用户侧验收未回报**：`pytest -q`（预期 `88 passed`）、`tools\smoke_p1.py`（预期 9 步全绿）、`/docs` 双击复核第二次得 `409`——按项目约定第 9 条，这些命令由用户亲手跑。

候选队列（不影响当前目标）：

- `POST /api/report` 仍**没有**防重复保护（连点会落两条报告）；正解是 P2 前端提交后禁用按钮，不在后端做启发式判重。
- P1 新增 `POST /api/plan`、`POST /api/plan/nodes` 两个接口（已补进 SPEC 第 11 节），SPEC 第 16 节把「改契约」列为 Ask first，**尚未经用户追认**。
- 到 P4 前需用户提供邮箱 SMTP 授权码（邮件提醒已定为启用）；用户那条留空的需求（原文「2、」后空白）默认不做，等其补。
- 本地库 `data/cadence.db` 有用户真实档案与手工验收的演示数据（3 个计划、节点 1–6、报告 1–4、1 条 pending 提案）——**不得清库**。
- 工作区三件未提交，均由用户决定，Agent 不擅自处理：① `AGENTS.md`（用户新增教学协议）；② `无标题-2026-09-14-2037.excalidraw`（仅清理 56 个 `isDeleted` 墓碑元素，已核实内容与基线一致）；③ `docs/代码串联图.excalidraw`（2026-09-15 重画到 P1 状态，结构自检 0 错 0 警，仍未进版本库）。

## 4. 稳定边界与重新打开条件

- 本产品只做**方向层**：学习过程追踪（时长、视频进度、笔记、打卡、番茄钟）已明确排除，不得重新实现。
- 状态变更必须经 `backend/app/ledger.py`，不得直接 UPDATE 业务表；缺理由或对已作废记录动手，一律抛 `LedgerError`，不静默忽略。
- 业务规则集中在 `backend/app/plan.py`（状态机、落后量、阶段判定、周检查点判定、防重复），接口层只翻译 HTTP 状态码。
- 前端不得持久化业务状态，不得直连数据库或 LLM；业务规则在后端算完再给前端。
- LLM 只产出结构化提案，写入必须经用户裁定。
- 已定决策共 21 条见 `docs/SPEC.md` 第 18 节；除用户明确要求，不重新讨论。
- `无标题-2026-09-14-2037.excalidraw` 是用户手绘的原始设计图，只读，不得删除或改写。

## 5. 证据记录

| 编号/日期 | 来源、操作与环境 | 留存/访问 | 结论 | 适用范围/失效条件 |
|---|---|---|---|---|
| E-01 / 2026-09-14 | `cd backend; .\.venv\Scripts\python.exe -m pytest -q`（Python 3.14.7） | 测试文件在仓库内，命令可复跑 | 通过：10 passed（P0 台账） | 适用于 `ledger.py`、`schema.sql`、`test_ledger.py`；三者至今未变，故仍有效 |
| E-02 / 2026-09-14 | 建库 + 起 uvicorn + 请求 `/api/health`、`/openapi.json` | 命令可复跑 | 通过（当时） | **已失效**：`main.py` 在 P1 重写，现由 E-07、E-09 覆盖 |
| E-03 / 2026-09-15 | `pytest -q`（P1 代码刚完成时） | 命令可复跑 | 通过：78 passed | **已被 E-06 取代**（T19 改动了 `plan.py`、`main.py`） |
| E-04 / 2026-09-15 | 临时库起 uvicorn（8099）+ Invoke-RestMethod 走完闭环 → 查库 | 临时库与临时脚本已删 | 通过：落后量 5 天→0；非法迁移 400；缺 `parent_id` 400；台账含 before/after | **已被 E-07、E-09 取代**（基线 `7eee000`） |
| E-05 / 2026-09-15 | **用户亲手**在 `/docs` 操作（真 uvicorn + 自己的库）：建计划→建阶段→建 2 个检查点→3 次报告（含 1 次白点）→`GET /api/plan` | 数据留在用户本地库，可随时复查 | 通过：节点按状态机推进、落后量 5 天→0、产出 1 条 pending `stage_advance` 提案、`GET /api/plan` 七项字段与预测逐项吻合 | 基线 `7eee000`；T19 之后由 E-09 端到端复现 |
| E-06 / 2026-09-15 | `cd backend; .\.venv\Scripts\python.exe -m pytest -q`（T19 之后） | 测试文件在仓库内，可复跑 | 通过：88 passed | 基线 `2904831`；`backend/` 下代码或测试变更后失效 |
| E-07 / 2026-09-15 | 临时库起 uvicorn（8099）+ 五种连发情形：同名连点两次、带空格标题、换标题、已收尾后重建同名、同名阶段 | 临时库已删除；复跑方式：对同一 `POST /api/plan/nodes` 连发两次 | 通过：未收尾同名 409（带中文原因）、空格标题 409、换标题 201、已收尾后重建 201、同名阶段 409 | 基线 `2904831`；`plan.py`、`main.py` 变更后失效 |
| E-08 / 2026-09-15 | `python tools/show_db.py`（对用户真实库只读导出）+ 一次只读连接写库试验 | 脚本在仓库内，可复跑 | 通过：打印结果与已知事实逐项一致（3 计划 / plan 3 树 2-2 收尾 / 4 报告 / 台账 10–19 带理由 / 1 pending 提案）；写库被 SQLite 拒（`attempt to write a readonly database`）；用户库 mtime 未变 | 纯只读，可反复跑；`app/plan.py` 变更后需重看 |
| E-09 / 2026-09-15 | `python tools/smoke_p1.py`（自起临时库 + 空闲端口） | 脚本在仓库内，可复跑；临时库跑完自动删 | 通过：9 步全绿（落后量 5→0、阶段收尾产出提案、未收尾同名 409、已收尾同名 201）；用户库 mtime 未变、无残留库与残留进程 | 基线 `9f95a38`；`plan.py`、`main.py` 或接口契约变更后失效 |

## 6. 启动、验收与上下文

```powershell
cd D:\cadence\backend
.\.venv\Scripts\python.exe -m app.db init         # 建库（可重复执行）
.\.venv\Scripts\python.exe -m pytest -q           # 预期 88 passed
.\.venv\Scripts\python.exe tools\show_db.py       # 只读看库：计划树 / 报告 / 台账 / 提案
.\.venv\Scripts\python.exe tools\smoke_p1.py      # 闭环冒烟：自起临时库跑 9 步，不动真实数据
.\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8000   # 手动验收用，/docs 可打开
```

**待用户执行的验收**：先重启 8000 服务（旧进程不会自动加载新代码），跑 `pytest -q` 预期 `88 passed`；再到 `/docs` 对 3 号计划的阶段 4 连点两次建同名检查点，第二次应得 `409`。

| 任务类型 | 必读文件 |
|---|---|
| P1 回归 / 验收 | `backend/app/plan.py`、`backend/app/main.py`、`backend/tools/`、`tasks/todo.md`（T4–T6、T19） |
| 下一步（P2 前端） | `tasks/plan.md`（P2）、`docs/SPEC.md` 第 10、11 节 |
| 产品/架构变更 | `docs/SPEC.md` 第 10、11、18 节 |
| 需求背景 | `docs/SPEC.md` 第 1–9 节、`docs/U2-触达详解.md`、`docs/未决项讨论.md` |

## 7. 给下一个 Agent 的启动提示

1. 完整读取本文件并核对 Git、工作树与未提交改动。
2. 只读取当前目标路由的源码、测试与规则。
3. 证据沿用规则：E-01 在 `ledger.py`/`schema.sql`/`test_ledger.py` 未变时可沿用；E-06 至 E-09 在 `plan.py`/`main.py`/`tools/` 未变时可沿用；E-02、E-03、E-04 已失效或被取代，不要引用。
4. 用户已亲手走通 P1 闭环（E-05），但其 `pytest` 与冒烟结果尚未回报；**开工任何新工作前，必须先报三句话计划并等确认**。
5. 遵守项目 AGENTS.md 的教学协议：讲代码先给全景（怎么跑起来、请求怎么流、数据谁持有），分清现状与蓝图，每条知识配一个用户能亲手执行的动作，并让用户复述。
6. 每次提交 ≤200 行有效改动；不因工作区缺少外部附件而推断附件从未提供，不把历史阻塞写回当前阻塞。
7. 不删除分支、工作树或用户数据，除非用户明确授权（`data/cadence.db` 里有用户真实档案与手工验收数据，不得清库）。
