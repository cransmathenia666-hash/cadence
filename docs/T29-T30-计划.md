# T29 / T30 执行计划（交主对话）

来源：2026-09-18 侧窗讨论（用户走查后拍板）。**已落进 `tasks/todo.md`** 的 v0.12 变更行与 T29、T30 条目——本文件是给主对话看的执行版：先做什么、卡在哪、哪些还没定。别重问已经拍板的事。

## 一、已经拍板的（原话在 todo 条目里，不要重新征求）

1. **判资料页只装判资料的**（四问输入 + 当次答案 + 历史记录）；「学什么」那条路的产物一律不进这页。
2. **蓝图待批单独一页**——他明确否掉了我推荐的「并进计划页」。
3. **推进提案整类删**：规则自动产、批准不改任何东西；手动收尾计划页已经有了。落后的「重排」降级成计划页上的一句提醒，不再是要裁定的条目。
4. **改节点字段 = 原地改 + 台账记一条流水**（谁 / 何时 / 改前改后 / 理由），**id 不变**；**不用「取代」**。

## 二、开工前必须先做（不然会白做）

- **改 SPEC**：决策 28（提案按 kind 分流裁定）、决策 29（两个正式页）、决策 30 里「阶段完成即产推进提案」那半句；再给「节点改字段」补一条第 18 节决策。项目约定是先落盘规格再开工。
- **处置存量**：真实库里还有 **5 条 pending 的推进提案**（计划 #2，提案 #4–#8）。整类删之前先定它们的去向——一次性标终态，还是留着只读。别让它们在新逻辑下变成孤儿。
- **`/report` 与 proposal 的关系要一起看**：推进提案的生产者挂在报告与任务收尾两条路上，摘的时候两条都要摘干净。

## 三、两件要他回答的（他答了才能收尾）

1. 判资料页要不要留一条**只读的历史记录**？（裁定环节删掉之后，过去判过的资料就查不到了。）
2. 计划页那块对话（T28）要不要**真能改计划**？他此前说过「聊是本体、改计划不是目的」，这次只答了写入口的留痕口径。

## 四、建议顺序

**T29 先做**（纯删减 + 挪页面，风险低，且先把「待裁定」腾干净）→ **T30 再做**（动了台账写入语义，按纪律加跑 `tools\smoke_p1.py`）→ 最后才谈「把对话接上能改」。

## 五、落点（看过代码，别找错地方）

- **T29**：`plan.maybe_stage_advance_proposal` 与它的两个调用口（`submit_report`、`_propose_after_child_settles`）；`plan.py` 里落后判定的重排提案生产者；`proposals.list_pending` 的 kind 分流与 `frontend/app/proposals/page.tsx` 的分流渲染；四问入口（`askMaterial` 那条 `POST /api/requests` 的 evaluate 分支）要独立成页。
- **T30**：在 `plan.add_node` 旁边加一个改字段的动作，走 `ledger.log_event` 记改前改后；**注意 `ledger.py` 里 `plan_node` 是 `supports_lifecycle=False`**，`ledger.supersede` / `void` 会直接抛错——能用的是 `log_event` 加一次原地 `UPDATE`，理由必填。路由照 `POST /api/plan/nodes/{id}/check|skip|deliverable` 那三条的样子加一条。

## 六、别踩的坑

- 「取代」那条路在 `plan_node` 上走不通**不是偏好，是现有代码会拒绝**（决策 22：节点的「不算数」由 `skipped` 表达）。所以 T30 的实现只能走「原地改 + 流水」。
- 计划页那块对话现在是「**只说话与建议，不改计划结构**」（T28 的设计）。他是专程把「聊」和「改」分成两件事的——T30 没做完之前别顺手让对话去写计划。
- T26 的 `built.notes` 里那句「它要交的东西没能写进去」，根子就是 T30 缺的那条写入口；T30 做完之后，这条限制和它相关的文案要一起回头改。