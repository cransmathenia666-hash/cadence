# 未决项讨论

> 本文件用于交付讨论内容：在当前环境下，聊天回复的正文似乎无法正常显示（多次确认如此），因此把内容写进文件。以后讨论未定项都累积到本文件。

## 一、未定项排序（按「卡住哪个阶段」）

- **不卡任何事**，随时能定、也可以一直不定：项目名、Markdown 导出。
- **卡 P2（前端开工）之前**：前端语言用 TypeScript 还是 JavaScript。
- **卡 P3（决策入口）之前**：LLM 用哪个 provider、预算与调用上限。
- **卡 P4（触达）之前**：U2 通道档位（档 0 被动 / 档 1 本地通知 / 档 2 本地定时+邮件 / 档 3 云端常驻）。
- **上线才需要**：上线形态（先本地还是直接上云）、认证（上线前必须加，至少一个 token）。
- **基本已定**：联网找资料按甲档起步（不联网），乙档以后再说。

讨论顺序建议：先 LLM，再 U2，最后上线那两条。

---

## 二、当前议题：LLM 与预算

### 结论先行

钱几乎不是问题。真正要定的是两条纪律。

### 成本量级

- 每周大约 **5 到 15 次**模型调用。
- 每次输入约 2–4 千 tokens（长期档案五类 + 计划表现状 + 历史摘要），输出约 0.5–1.5 千 tokens。
- 按便宜模型的价格量级，这是**分级到角级**的钱。具体单价你选定 provider 后核一下，我不编数字。

所以重点不是省钱，而是纪律。

### 两条纪律

**第一条：只在低频环节调，绝不在高频动作里调。**

可以调模型的只有四处：提交输入、生成候选、拆分阶段、压缩对话。

一律不调的：页面加载、打开计划表、提交报告、台账读写。

这一条直接决定成本量级，也决定页面快不快。

**第二条：调用上限 + 失败即报错，不猜。**

- 设一个每周调用上限（建议先 **50 次/周**），超了就停下来告诉你。
- 这是防呆：真正烧额度的不是正常使用，是某个 bug 导致的循环重试。
- 结构化输出失败只重试一次；再失败就如实把原文报给你，**不许猜一个填进去**。

### 顺带一个白捡的好处

因为之前定了「确定性优先」，模型挂掉的时候，**计划表、报告、台账、落后量这些照常能用**——只有「找」（生成候选）和「判」（四问判断）两个功能不可用。

系统是**降级**的，不是瘫掉的。这条以后可以写进项目介绍里。

### 工程上的准备

provider 在规格里已经抽成接口（`backend/app/llm.py`），换模型只改配置，不碰业务代码。测试用假 provider 打桩，不烧钱。

### 需要你定

1. **用谁的 key**：你已有的 commandcode 提供方，还是单开一个便宜的 API key。
2. **是否接受两条纪律**及其数值（每周 50 次上限 + 失败即报错不猜），或者你想改成别的数。

我的猜测：你会用已有的提供方，并接受这两条纪律。

---

## 三、其余仍未定（供后续讨论）

1. U2 通道档位（我押档 2：本地定时 + 邮件）。
2. 联网 provider 何时从甲档升到乙档。
3. Markdown 导出要不要做、导到哪个目录。
4. 项目名。
5. 上线形态：先纯本地跑通，还是现在就按 Vercel + Render + 托管 Postgres 走（我建议先本地）。
6. 前端语言：TypeScript 还是 JavaScript（我建议 TS）。
7. 认证：上线前必须加，本地开发不做。
